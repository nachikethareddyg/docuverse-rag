# AWS IAM

AWS Identity and Access Management (IAM) helps control access to AWS resources.
IAM users, groups, roles, and policies define who can perform actions against
specific services and resources.

For document systems, IAM permissions can limit which applications may read
objects from S3, write logs, or access other AWS services. Least privilege is a
common design goal when creating IAM policies.
